This software requires the following libraries:

	- Qt >= 4.4

To compile this software, type:

	qmake
	make

To install this software, type:

	make install

To build on Mac OS X, type:

	qmake -spec macx-g++
	make

then an universal binary application will be built.